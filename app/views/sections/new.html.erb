<div class="container">
  <div class="row">
    <div class="col-md-12">
      <%= simple_form_for(@section, html: { class: 'form-horizontal' }) do |f| %>
        <%= f.input :title, label: "ES小项的标题" %>
        <%= f.input :content,label: "内容",  as: :text  %>
        <%= f.button :submit, "提交", class: "btn btn-info" %>
      <% end %>
    </div>
  </div>
</div>
